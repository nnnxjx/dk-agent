# 项目面试问答手册 — Nest-Agent 多 Agent 编排平台

> 背诵方法：每个问题先记住第一句结论，再记住中间的流程故事，最后记住一个坑或者取舍。面试时按这个顺序说，就很像自己做的。
> 本次深化版新增了大量面试官爱深挖的追问，全部用文字讲清楚，不贴代码。

---

## 0. 30 秒自我介绍

我自己做了一个多 Agent 问答和工作流平台。后端用 NestJS，前端用 React。核心能力有两个：第一是智能路由模式，用户直接提问，系统里的大模型主管会自动判断这个问题该派给搜索研究员还是直接回答；第二是自定义工作流模式，用户可以在前端画一张流程图，规定先让哪个 Agent 做、再调哪个工具、满足什么条件走哪个分支，后端会按这张图执行。

对话是流式输出的，遵循 AG-UI 这套业界事件规范，前端能实时看到大模型正在吐字、正在调哪个工具、参数是什么、结果是什么，还能随时点停止。周边还做了多家大模型切换、联网搜索和知识库检索两个工具、长对话的记忆压缩、登录鉴权和多租户隔离、Redis 缓存。数据库用了 MySQL 存业务数据，Milvus 存向量，Redis 做缓存，全部用 Docker 一键拉起。

---

## 1. 技术选型

### Q1：为什么后端选 NestJS，而不是 Express？

结论是这个项目模块多、生命周期长，需要框架层面的约束。

首先是模块划分很自然。认证、对话、Agent 编排、知识库、大模型、工具、缓存，七个领域各自独立成模块，由根模块统一装配，互相通过依赖注入拿服务，比在手写框架里维护一堆全局单例清晰很多。

其次是横切逻辑多。登录鉴权、租户校验、参数校验、全局异常处理，这些如果散落在每个接口里会很乱。NestJS 的守卫、装饰器、过滤器正好解决这个问题：请求先进鉴权守卫解析用户，再进租户守卫确认租户，最后才到业务代码，业务代码只需要声明我需要当前用户和租户编号即可。参数不对或者抛异常，也由统一的过滤器转成标准错误格式。

最后是这些 Agent 和向量库服务都是有状态的长连接单例，框架的单例管理和启动钩子比自己维护全局变量可靠。代价是样板代码多一些，但对这种偏平台型的项目是值得的。

深挖：NestJS 的依赖注入在这里解决了什么具体痛点？

比如 Agent 服务同时需要大模型工厂、工具注册表、主管工厂、DAG 引擎、工作流服务、知识库服务六个依赖，如果手写，需要自己控制初始化顺序，比如向量库没连上时知识库服务不能先用。有了框架，这些服务的构造顺序和单例复用由容器保证，启动时的健康检查也可以挂在模块初始化钩子里。而且守卫和装饰器让租户透传这件事从每个接口的三五行重复代码变成一次声明，少了很多漏传租户导致串数据的风险。

### Q2：为什么用 LangGraph 做编排，不自己写流程控制？

结论是 Supervisor 这种带环的路由，自己写循环很容易写崩。

可以这样理解：Supervisor 模式是一个圈，主管派活给研究员，研究员做完回来找主管，主管再看要不要派给别人或者结束。这个圈要处理消息合并、防止无限循环、支持中途取消、还要把每一步的 token 流出来。自己写循环每一项都要手写，很容易漏。

LangGraph 提供的就是一张有向图加共享消息链，路由就是一句话：下一步去谁那里，并附带要更新的消息。DAG 模式也是同一个思想，只是这张图不是写死的，而是前端存下来的节点和边，后端读出来动态编译成图。这样两套编排共用同一套状态、重试和取消机制。而且单个 Agent 的思考加调工具这个循环，框架里已经有现成的智能体实现，不用重复造轮子。

深挖：手写循环最容易崩的三个地方是什么？

第一是消息合并，子 Agent 返回的消息要追加而不是覆盖，还要保留工具调用和工具结果的配对关系，自己拼很容易把顺序搞乱。第二是中断，一个循环里套了大模型调用又套了工具调用，取消信号要层层透传，漏一层就会出现前端点了停止、后端还在烧 token 的情况。第三是无限循环，主管和子 Agent 互相踢皮球时要有轮数上限和结束语义。用图框架后，这三件事分别由共享消息注解、统一取消配置和递归上限兜底，自己只需要写路由判断。

### Q3：流式为什么用 SSE，不用 WebSocket？

结论是对话是单向推送，用 SSE 更轻。

对话的场景就是浏览器发一句话，服务器源源不断往回推文字和工具事件，不需要浏览器反过来频繁发二进制帧。SSE 本质就是一个长期不关闭的 HTTP 请求，能直接复用现有的登录头、跨域配置和网关缓冲控制，后端只需要关掉代理缓冲即可。

AG-UI 协议本身也是按 SSE 的帧格式设计的，一个事件加一段数据，前端按空行切帧就行。如果换成 WebSocket，还要自己做心跳、重连、鉴权，得不偿失。中断问题也用 HTTP 语义解决了，一种是浏览器直接关连接，一种是调删除运行记录的显式接口，两种都会触发同一个取消注册表。

深挖：SSE 有什么先天弱点？为什么这个项目能接受？

弱点有三个。一是浏览器对同一域名 SSE 连接数有限制，多会话并发会排队，但本项目是单会话单运行，顶替机制保证同一时间只有一个流，所以碰不到。二是 SSE 是文本协议，传二进制不方便，但对话全是文本和 JSON，刚好对口。三是断线重连要自己做位点续传，本项目用落库加刷新对齐代替续传，中断后刷新页面看的是已落库的半句话加后缀，而不是断点续流，产品上可接受。如果以后要做多模态大文件，就得重新评估。

### Q4：为什么同时用 MySQL、Redis、Milvus？

结论是三种数据形态不一样，硬塞到一种库里都会别扭。

MySQL 存的是关系型源数据，也就是用户、会话、消息、工作流定义、知识库元信息这些，特点是强一致、要按租户隔离、要排序分页，所以每张表都有租户编号索引。

Redis 存的是热点读数据，主要是会话详情和消息列表。因为对话时每次都要读历史，直接查库太频繁，所以加一层带过期时间的缓存。消息是只增不改的，追加写缓存很安全，会话标题或摘要变了就删缓存懒加载。

Milvus 存的是向量。关系库做不了近似最近邻检索，所以知识库的文本向量单独放向量库。每个知识库建一个独立集合，集合名里带租户信息，检索时再按知识库编号过滤，双重隔离。向量维度是 1024，和选用的中文向量模型对齐。

深挖：每个知识库建一个集合有什么代价？什么时候该换成单集合？

好处是删除知识库时直接删集合，干净彻底，也天然隔离。但代价是集合数量会随知识库膨胀，向量库的集合和索引都是有成本的，几千个小库会浪费很多内存和加载时间。如果租户和库的数量级变大，更优的是单大集合加租户和库编号联合过滤，删除时按过滤删向量。现在的方案是为演示期简单可靠选的，面试时主动说出这个 trade off 会很加分。

### Q5：参数校验为什么用 Zod？

结论是工作流的请求体是动态 JSON，Zod 表达力更强。

对话和工作流的请求里有很多自由字段，比如节点的配置是一个任意键值的对象，边上还有可选的条件文字。用传统的装饰器校验要写很多类，而 Zod 可以直接声明对象里有哪些必填、哪些可选、枚举有哪些值，在接口入口解析一次就行，失败会抛统一的校验异常，再由全局过滤器转成带字段路径的错误，前端一看就知道哪个字段填错了。

深挖：Zod 在这个项目里还承担了什么超出校验的职责？

它还承担了和大模型的契约。一是主管路由的结构化输出就是用 Zod 定义的枚举，强制大模型只能三选一，这是提示词约束之外的硬约束。二是工具的参数结构也是 Zod 定义的，大模型调工具时必须按这个结构传参，相当于函数调用的接口文档。所以 Zod 既是防前端传错，也是防大模型乱回。

---

## 2. Supervisor 智能路由模式

### Q6：主管是怎么决定派给谁的？

可以把主管想象成一个只会做选择题的考官。

每次用户发消息，主管会先看一遍完整历史，然后被要求只能输出三选一：派给研究员、派给直接回答者、或者任务结束，还要附一句话理由。这个强制三选一是通过结构化输出实现的，大模型想自由发挥也不行，只能填这几个选项。

如果选了研究员，流程就跳到研究员节点，研究员做完后无条件回来找主管，主管再看一次：是不是已经答完整了，完整了就结束，没完整就再派。开始节点直接连主管，研究员的出口也连回主管，这样就形成了一个圈。为了防止两个节点互相踢皮球，执行时设了一个最多 25 轮的上限。

还有一个细节，主管每次路由都会往消息链里记一笔说我派给了谁，但直接回答者在最终组织语言时会把这一笔过滤掉，不然用户会看到奇怪的内部调度记录。

深挖：主管每次路由都要多花一次大模型调用，值得吗？

值得，但是有成本。好处是路由逻辑不用写死规则，可解释性强，加一个新 Agent 只需要把名字和介绍拼进提示词，不用改代码。坏处是每次用户提问至少多一次短调用，简单寒暄也会先过主管。优化方向是加一层轻量预路由，比如正则命中寒暄直接走回答者，或者缓存高频问题的路由结果。本项目为简单起见全量走主管，面试时要能算清这笔账。

深挖：结构化输出失败了会怎样？

大模型偶尔会返回不符合枚举的值，或者网络超时。这时框架层会抛异常，被执行入口捕获后转成运行出错事件，前端弹错。缓解办法是给主管调用加重试和降级，比如失败一次就默认派给研究员，因为研究员能力最全，宁可慢不可错。现在项目里没有专门的重试，这是可以承认的改进点。

深挖：如果以后有五个 Agent，主管还撑得住吗？

撑得住，但提示词会变长，路由准确率会下降。工程上的做法是分组，先由主管选大类，再由二级主管选具体执行者，或者把 Agent 介绍写得更互斥，减少模糊地带。另外路由选项是动态拼的，加 Agent 不用改图结构，只需要注册新节点并声明出口，这是当初把路由选项做成动态数组的原因。

### Q7：研究员和直接回答者有什么区别？工具是怎么配的？

研究员是全副武装的，配了两个工具，一个查内部知识库，一个查互联网，提示词里明确要求先查知识库，查不到再联网，而且要引用来源。直接回答者是轻量级的，不带任何工具，专门处理寒暄和常识问题，速度更快更便宜。

工具分配上有个租户隔离的讲究。联网搜索是全局单例，谁都能用。但知识库检索不能全局共享，因为每个租户的知识库不一样。所以每次来一个请求，后端会现场创建一个绑定了当前租户编号的知识库检索工具，再塞给研究员。这样研究员只能看到当前租户的库，不会串。DAG 模式下则是把所有工具摆在一张表里，每个节点按名字挑自己需要的。

深挖：为什么知识库检索工具不能做成单例？

因为单例存不住租户。工具的执行函数闭包里要带着当前请求的租户编号，去列出该租户的库。如果做成全局单例，要么所有租户看到同一个库，要么要用线程本地变量传租户，在 Node 单线程事件循环里很容易串。现场创建的成本很低，就是包一层函数，换来的是隔离的绝对安全。这是一个典型的用小成本换大安全的设计。

深挖：研究员的提示词为什么强调先查知识库再联网？

这是为了压幻觉和省钱。内部知识往往有时效和口径要求，优先用自己的资料能保证口径一致。联网结果杂且贵，只有知识库没命中或者问题明显问的是实时新闻时才用。提示词加路由规则双保险，就是怕大模型偷懒直接联网。

### Q8：主管的路由规则是什么？

规则只有三条，但是中英文各写了一遍，防止中文问题被漏掉。

第一，用户一提到知识库、内部文档、查资料这类词，不管别的，一律先派给研究员。第二，只有纯寒暄比如你好、谢谢，才派给直接回答者。第三，如果子 Agent 已经给出了完整答案，主管就直接结束，不再转圈。每次路由的理由都会记下来，方便排查为什么某句话走错了。

深挖：理由字段有什么用？只是为了好看吗？

不只是好看。一是排查时能看到主管当时是怎么想的，比如把查报价派给了回答者，理由里可能写着我以为这是寒暄，就能定位是提示词问题还是模型问题。二是以后做评测时，可以拿理由做人工抽检的依据。三是给前端预留了展示位，以后可以把路由理由做成可展开的思考过程，增强可解释性。

---

## 3. DAG 自定义工作流模式

### Q9：用户画的工作流是怎么存、怎么跑的？

前端的工作流就是两张清单：一张是节点清单，每个节点有编号、类型、名字和配置；一张是边清单，说明从谁连到谁，附带可选的条件文字。这两张清单以 JSON 形式存在关系库里。

用户在对话页顶部的下拉框里选了某个工作流再提问，请求里就会带上工作流编号。后端执行时先看有没有这个编号，有就走 DAG 模式，没有就走主管模式。走 DAG 之前会先按编号加租户校验把工作流查出来，防止越权读到别人的图。因为自定义链可能比主管模式更长，所以递归上限放宽到 50。

深挖：工作流的校验现在够吗？缺了什么？

现在的校验只保证字段齐全，不保证图是合法的。比如没有开始节点、开始后面没接人、有孤岛节点、条件节点只有一条边、指向了不存在的节点，这些都能存进去，运行时才会暴露。生产级应该在保存时做图校验：必须有且仅有一个开始和结束、所有节点可达、条件节点的每条出边都有条件文字、工具节点引用的工具必须存在。面试时主动说出这一层，说明你有平台思维。

深挖：全局变量字段存了但没用，怎么看？

工作流表里确实有个全局变量列，但执行引擎现在没读它。这是预留的扩展点，本意是让节点配置里可以用变量占位，比如搜索关键词复用用户输入。现在没做，诚实承认比硬圆好，并给出做法：执行前把变量注入上下文，工具节点输入做模板替换。这样反而显得你对代码现状很清楚。

### Q10：五种节点各是干什么的？后端怎么按图执行？

先说五种类型。开始和结束只是标记，不干活。Agent 节点是一个能调工具的小智能体。工具节点是直接调某个工具一次。条件节点是岔路口，根据上一句话的内容决定走哪条边。

执行原理分三步。第一步把所有边建成邻接表，也就是每个节点后面跟了哪些边，一查就知道。第二步跳过开始和结束，给其余每个节点注册一个执行函数，并声明它的出口有哪些，指向结束的出口会被映射成图的终点。第三步找到从开始节点出发的那条边，把图的入口接过去，编译成可执行图。

每个节点的执行函数长得一样：先广播说我开始了，再检查有没有被取消，然后按类型干活。Agent 节点按配置挑工具并现场组装一个小智能体来跑；工具节点直接调一次工具，把结果包装成一条人类消息追加到消息链里，相当于告诉后面的人工具说了什么；条件节点去看最后一条消息里包不包含某条边上的关键词，包含就走那条，都不包含就走第一条，然后直接跳过去。任何节点抛异常都不会炸掉整张图，而是记一条日志并追加一条报错消息继续往下走，最后统一广播说我结束了。

深挖：条件分支是怎么跳过去的？和普通顺序执行有什么区别？

普通节点做完后，引擎看它的出边数量，一条就默认往下走。而条件节点是主动下命令，直接指定下一跳是谁，并把当前消息原样带过去。这就是命令式跳转和静态连线的区别。好处是分支逻辑可以写任意代码，坏处是分支多了之后图的静态结构就看不清了，调试要靠日志。

深挖：单节点失败为什么不让整张图失败？

因为工作流很多是长链，一个搜索超时就全盘重来成本太高。现在的策略是把错误也当成一条消息追加，让下游的 Agent 看到上游报错了，自己决定是换个关键词重试还是如实告诉用户。这是一种容错优先的设计，适合演示和弱依赖场景。如果是支付下单这种强一致场景，就该改成失败即停并补偿，这一点要能分清。

深挖：DAG 支持并行分支吗？

数据结构上支持多出边，但现在的执行函数对多出边没有显式扇出扇入语义，实际还是按命令跳转一次走一条。真要做并行，需要引入扇出收集器，等待多分支都完成再合并消息。现在没做，是为了控制复杂度，面试时可以说出扇出扇入和合并策略是下一步。

### Q11：条件节点现在是关键词匹配，有什么问题？怎么改进？

问题很直白：换个说法就认不出来，而且只看最后一句话，上文提到的信息用不上。

改进可以分三档说。最省事的是支持多个关键词或正则，命中一个就算。中等投入是让条件节点也调一次小模型做判断，相当于把主管那套选择题搬过来，只是选项变成几条边。彻底的做法是把条件写成表达式，前端配表单，后端按表达式求值。现在用关键词纯粹是为了零成本可演示，面试时主动说出这三档会很加分。

深挖：关键词匹配最容易出的线上事故是什么？

一是大小写和简繁体，英文条件很容易因为大小写 miss，所以要统一转小写。二是子串误伤，比如条件写个单字，很容易在无关句子里命中，导致走错分支。三是只看最后一条消息，如果最后一条是工具的长结果，关键词可能藏在中间，截断或换行会导致 miss。缓解办法是限定匹配窗口、要求词边界、命中后记日志方便回放。

---

## 4. 流式协议与 token 级输出

### Q12：AG-UI 事件到底长什么样？

可以记成三组。第一组是生命周期：运行开始、步骤开始、步骤结束、运行结束，异常是运行出错，取消是运行取消并带原因，区分是用户关网页、点了停止还是被新消息顶掉。第二组是文本三段式：消息开始、消息增量、消息结束，前端靠消息编号把增量拼成一句话。第三组是工具四段式：工具开始、参数增量、工具结束、工具结果。序列化就是在每段前面加个事件名，后面跟 JSON，前端按空行切帧解析，和常见前端 Agent 套件是兼容的。

深挖：为什么文本和工具都要拆成开始增量结束三段，而不是一次发完？

因为大模型是逐字吐的，工具参数也是逐片流出来的。如果等整段攒完再发，首字延迟会很高，用户体感就是点了没反应。拆成三段后，前端收到开始就建气泡占位，收到增量就往里拼，收到结束就收尾，流畅很多。而且取消时也能靠是否收到结束来判断哪些气泡是半截的，补后缀落库。

深挖：事件编号是怎么保证前后端对得上的？

文本消息和工具调用都有全局唯一编号，开始和增量和结束用同一个编号串起来。前端用编号做字典，增量来了就往对应气泡里追加。工具结果用工具编号关联回工具卡片。这样即使网络乱序，只要编号对，就不会把张三的参数拼到李四身上。后端用几个集合记录哪些编号发过开始、哪些发过结束，保证不重不漏。

### Q13：token 级流是怎么从图框架里抠出来的？这是最常问的

核心就一句话：用框架的事件流模式订阅到底层大模型的每一次吐字。

后端拿到图之后，不是一次性等结果，而是以事件流版本二去订阅。框架会把内部一切动静都报出来：大模型吐了一个 token、调了一次工具、工具返回了、某个节点结束了，都有对应的事件。后端要做的就是翻译官。

大模型流式事件来了，先看是不是主管节点的，直接扔掉，不然前端会看到内部调度碎碎念。再看这一块是工具参数还是纯文本，如果是工具参数就翻译成工具开始和参数增量事件，如果是纯文本就翻译成文本增量事件。大模型一次调用结束了，就补上工具结束和文本结束。工具执行完了，就发工具结果。某个顶层节点结束了，就发步骤结束。

还有一个节点名的问题。因为研究员内部还有子图，框架报的节点名可能是内部的小名字，而不是研究员。所以后端会优先看检查点命名空间里的顶层名字，拿不到才用原始节点名，这样前端步骤条上显示的才是研究员这种人能看懂的名字。

深挖：工具参数是碎片来的，前端怎么拼出完整参数？

大模型传参数不是一次给全，而是像挤牙膏一样一片一片给，每片只有编号和一小段字符串。后端不解析，直接把每片原文透传，前端按工具编号把碎片拼起来展示。好处是首字延迟低，坏处是中途看到的是半截 JSON。等大模型这次调用结束，后端会发工具结束，前端就知道参数齐了。工具真正执行时拿的是框架拼好的完整对象，和展示通道是两条线，互不干扰。

深挖：怎么区分顶层节点结束和子图内部结束？

靠两个信号。一是检查点命名空间是否为空，为空说明不在子图里。二是是否有步骤序号。只有两者都满足，才认为是顶层结束并发步骤结束事件。否则子图内部每结束一次都发，前端步骤条会疯狂闪烁。这个判断是流式状态机里最容易写错的地方，也是排查转圈卡死时首先要看的。

深挖：主管节点的流为什么要丢掉？丢了会不会丢信息？

主管的输出只是路由选择和理由，不是给用户看的答案。如果不丢，前端会看到一闪而过的内部调度语。丢的是展示，不是状态，因为主管的路由结果已经通过跳转生效，消息链里也记了一笔。直接回答者已经把那一笔过滤了，所以用户既看不到也不受影响。

### Q14：国产模型不按标准调工具怎么办？子 Agent 的废话怎么过滤的？

这是两个实战坑。

第一个是有些模型不走标准的工具参数通道，而是直接在文本里吐类似工具调用的标签。后果是前端会看到一堆标签明文。解法是准备一个缓冲区，只要发现文本里出现了这种标签，就先不发，攒起来，等大模型这次说完了再把整块标签和残留尖括号一次性洗掉，只把干净文本发出去。

第二个是子智能体在调工具之前会复述一大段内心独白，其实是它在读提示词，用户不该看到。解法是看事件来自哪一层，如果是子图内部的模型在工具还没做完时说的话，就先暂存在一个按节点隔离的小本子上。一旦发现它真的开始调工具了，就把小本子撕掉。等工具做完了，它后面说的话才是真正要给用户的，再正常放行。正在调工具时顺带说出来的同步文本也直接跳过。

深挖：缓冲区洗标签会不会误伤正常文本？

有可能，比如用户真的在聊标签语法。缓解是只在疑似工具调用的上下文里启用缓冲，比如研究员节点且文本里出现标签关键词，而不是全局一刀切。而且清洗只去掉整块标签和残留尖括号，正常文字原样放行。更彻底的是换支持原生函数调用的模型，或者在提示词里明确不准用标签格式。

深挖：小本子暂存策略会不会把有用思考也吞了？

会，所以这是一个产品取舍。现在的策略是工具做完之前子图的话一律不发，工具做完之后全发。这保证了用户看不到复读，但也看不到真正的推理过程。如果产品想展示思考过程，应该单独开一条思考事件通道，而不是混在正文里。现在是为了正文干净牺牲了可解释性，面试时要能说出这个权衡。

---

## 5. 停止生成与取消

### Q15：点一下停止按钮，背后发生了什么？

前端为了体感快，做了三个动作。第一是立刻关掉浏览器这边的流连接，服务器会感知到请求断了。第二是再补发一个删除运行记录的显式请求，防止某些代理不透传断开信号。第三是本地立刻把已经吐出来的半句话收尾展示，整个过程在百毫秒内完成，后到的取消确认会被幂等守卫挡掉，不会重复收尾。

后端收到请求时会创建一个运行编号并配一个取消控制器，把取消信号一路传下去：传给图的事件流、传给每个节点的执行配置、再传给大模型和工具的调用。任何地方检查到信号被拉起，就会抛一个取消异常。主管模式和 DAG 模式的每个节点入口都会先检查一次，所以取消是很灵敏的。顶层的控制器会识别这是用户取消还是真报错，取消就发运行取消事件，而不是运行出错，前端就不会弹红字。

还有一个同会话顶替机制：同一个会话同一时间只允许一个运行在跑，新消息来了会自动把旧运行取消掉，防止两股文本互相覆盖。

深挖：为什么要双通道？只关连接不够吗？

只关连接在直连时够，但在经过网关、代理、移动网络时，对方可能不及时透传断开，服务器还在烧 token。显式删除接口走的是另一条正常请求，不依赖原连接是否断开，相当于双保险。两者汇入同一个注册表，谁先到谁先生效，另一个到达时发现已经取消就直接幂等返回。

深挖：取消信号到底传了几层？漏一层会怎样？

至少三层：图的事件流订阅、每个节点的执行配置、大模型和工具的单次调用。漏掉事件流层，循环停不下来；漏掉节点层，当前节点做完还会进下一个；漏掉调用层，大模型那一次调用要等到吐完才停。所以每个节点入口和每次调用前都要检查一次，这是全链路透传的含义。

深挖：新消息顶掉旧运行是什么语义？旧运行的数据怎么办？

旧运行被取消后，已经收集到的半截文本也会按取消口径落库加后缀，新运行再正常跑。这样历史里会看到一条带停止后缀的旧回答加一条新回答，而不是旧回答凭空消失。这是为了保证刷新后和当时看到的一致，可追溯。

### Q16：取消后刷新页面会丢字吗？数据怎么保证一致？

不会丢，前后端是按同一套口径落库的。

后端不管正常结束还是被取消，收尾时都会做同一件事：把这次收集到的文本拼起来，如果是取消的就加一个已手动停止生成的后缀，然后存成一条助手消息，再刷新会话的更新时间保证排在最前。新会话还会异步生成标题，标题来自用户首问，所以取消也不影响。

前端也一样，收尾时把当前半句话加上同样后缀推进消息列表，和后端存的内容对得上。写流时还有保护，如果连接已经关了就直接丢弃，不会因为写关闭的连接而报错。运行记录删掉、监听摘掉、发一个结束帧，整次运行才算干净收场。

深挖：后缀为什么要前后端两份常量？改一处会怎样？

因为中断时前后端是各自独立收尾的，后端落库给刷新看，前端拼气泡给当下看，两边都要加。如果两边不一致，用户会看到当下和刷新后后缀不一样，显得很糙。所以必须保持同文，改的时候两处一起改，最好抽成共享契约。现在是硬编码一致，生产级可以做成配置下发。

深挖：收尾时如果落库失败了会怎样？

收尾的落库包了异常捕获，失败只记日志，不影响关流。坏处是刷新后可能少半句，但当下用户已经看到了。更严谨的做法是落库失败打标，下次进会话时提示有未持久化内容。现在为简单起见认了这个小概率窗口。

### Q17：取消注册表是内存的，多机部署怎么办？

这是预留的已知局限，单机没问题，多机要升级。

标准答案是把内存表换成发布订阅：取消时往以运行编号为主题的频道发一条消息，各台机器订阅后各自取消本地的控制器。同会话顶替需要一把分布式锁来协调，租户校验信息放共享哈希里。现在内存表里有一个十分钟的兜底过期清理，防止异常路径泄漏。

深挖：过期清理是谁触发的？有什么隐患？

现在清理函数是写好了但没有定时器自动调，靠人工或未来接入定时任务。这意味着极端情况下异常退出的运行记录会残留十分钟以上，好在只是内存条目，不影响正确性，只是占点内存。生产级应该在模块启动时起一个定时器，或者把运行记录本身存 Redis 加过期时间，让过期自动清理。

深挖：取消接口的租户校验为什么重要？

因为运行编号是全局唯一的，如果不校验归属，A 租户可以猜编号取消 B 租户的运行，造成拒绝服务。所以删除前先看这条运行是不是属于当前租户，不是就直接返回没取消，避免越权和信息泄露。

---

## 6. 长对话记忆

### Q18：滑动窗口加摘要是怎么配合的？

先记住两个数：窗口 10，阈值 20，都是可配置的。

每次要组装上下文时，先把全部消息拿出来。如果总数不到 10 条，就全量给大模型。如果超过 20 条，就触发一次摘要：把除最近 10 条之外的老消息拼成提示词，调一个低温度、不流式的模型压缩成一段摘要，存回会话表并记下摘要覆盖到了哪条消息，然后把摘要作为一条系统消息放在最前，后面再跟最近 10 条原文。如果总数在 10 到 20 之间，就只取最近 10 条，不触发摘要，这样能省 token。

摘要是增量式的，下次再超阈值时，会把已有摘要加上新增的老消息一起再压缩，而不是每次从头压。

深挖：摘要覆盖位存了但没真正用来跳过，为什么？

表里记了摘要覆盖到哪条消息，理想情况是下次只摘要新增部分，避免重复压缩。但现在每次都是把除最近 10 条之外的全量老消息再压一遍，覆盖位只起记录作用。这在长会话里会越压越贵。改进是按覆盖位切片，只压新增段，再和老摘要合并。这也是可以主动承认的优化点。

深挖：窗口和阈值怎么定？调大调小各有什么代价？

窗口大，上下文全，效果好，但 token 贵、首字慢。窗口小，便宜快，但容易忘事。阈值是触发摘要的线，定太低会频繁调摘要模型，反而更贵；定太高会导致 10 到 20 条之间长期只看窗口，老信息直接丢。现在的 10 和 20 是经验折中，生产级应该按会话长度和模型价格做成分级策略。

深挖：标题生成为什么单拎一次调用？

因为标题和正文是两个任务，混在一起会互相干扰。标题用低温度短调用，根据首问生成十几个字，异步跑不阻塞流。失败也不影响对话，只是标题保持默认。这种把副作用调用和主链路解耦的思想，在摘要、标题两处都用到了。

### Q19：Redis 缓存怎么做一致性的？

会话和消息列表各一把 key，都带一小时过期。读的时候先看缓存， miss 了再查库回填。写的时候分情况：新增消息后如果缓存还在，就直接往缓存尾巴追加一条，不用全量重查；改标题、改摘要、刷新时间后，就把会话缓存删掉，下次懒加载；删会话时两个 key 双删。因为消息没有编辑场景，只有追加，所以追加策略是安全的，摘要变更也不影响消息缓存。

深挖：消息缓存追加有什么并发坑？

两个请求同时追加时，可能出现读到同一份旧缓存、各追加一条、各写回，导致一条覆盖另一条。本项目靠单会话单运行机制大大降低了并发，一个会话同时只有一个写者在跑，所以基本碰不到。如果放开并发，就要加分布式锁或者改成列表结构用原子追加，而不是读改写。

深挖：一小时过期对长会话有什么影响？

过期后下次访问会回源查库，对用户无感，只是慢一次。但如果正好在摘要后过期，会话缓存被删加自然过期是双重保险，不会读到旧摘要。坏处是超长不活跃会话的缓存会被频繁重建，命中率低。生产级可以按活跃度续期，越活跃过期越长。

---

## 7. RAG 知识库

### Q20：从上传文件到能问答，中间经历了什么？

分成入库和检索两段。

入库时先按扩展名分发：PDF 用解析库抽文本，网页用解析器提纯正文，纯文本和表格直接读，JSON 解析后拼内容，网页链接会带浏览器头去抓并设 30 秒超时，不支持的类型直接报 400。然后按知识库配置的块大小和重叠量切块，默认一千字一块，重叠两百字，保证语义不断。再调中文向量模型转成 1024 维向量，批量写入向量库并更新文档计数。

检索时先把用户问题转成同模型的向量，再去对应集合里做近似检索，同时带上知识库编号的过滤条件，过滤条件里的编号会先做白名单清洗，防止注入。Agent 侧的检索工具会遍历当前租户的所有知识库，每个库各取几个，再按相似度全局排序截断，这样跨库也能召回。

深挖：为什么块大小默认一千、重叠两百？

一千字大约能包住一两个自然段，检索回来给大模型时信息密度适中。太小会碎，答案要拼多块；太大 noises 多，还浪费 token。重叠两百是为了不把一句话切两半，切点落在句子中间时，两边各留一点上下文，召回率更高。不同文档应该不同切法，表格按行切、代码按函数切，现在统一按字符切是简化版。

深挖：为什么选这个中文向量模型？维度对不上会怎样？

选它是看中多语言和长文本都还不错，且能走兼容接口，不用单独搭向量服务。维度必须和向量库建集合时的维度完全一致，否则写入或检索直接报错。所以代码里两处都写死 1024，一个是建集合，一个是模型配套，换模型必须两边一起换，还要重建历史数据，这是向量项目最痛的迁移成本。

深挖：Agent 侧为什么要遍历所有库再全局排序？

因为用户提问时不指定库，系统也不知道答案在哪个库。每个库各取几个再按分数排序，能保证跨库召回。代价是库多了会变慢，而且各库分数不完全可比。生产级可以加路由，先判断问题像哪个库的，再定向查，或者做混合检索加精排。现在的 fan out 是为库少时简单可靠。

### Q21：向量库连不上会拖垮整个站吗？

不会。服务启动时会先做一次带超时的健康检查，连不上就把客户端置空并记警告，不阻塞启动。每次真正操作前都会检查客户端是否可用，不可用就抛一个明确的中文错误，前端展示出来即可，不会变成 500 雪崩，也提供了一个可用性接口给健康检查用。

深挖：降级后用户体验是什么样的？

建库和上传会明确报错，告诉用户向量库不可用。对话不受影响，主管照样路由，研究员调知识库工具时会拿到错误文字，大模型会如实说知识库查不到，转而用联网或直接回答。这就是把硬依赖变成软依赖，核心链路永远可跑。

深挖：索引类型和参数怎么选的？

用的是暴力加倒排的组合，余弦相似度，倒排桶数一千。这是小数据量下召回准、实现稳的选择。数据量大了会慢，应该换成分层图索引，查询快但建索引慢、内存大。桶数越大召回越准但越慢，现在的一千是中等折中。面试时能说出数据量和索引的对应关系即可。

### Q22：上传的各种文件是怎么清洗的？有什么坑？

PDF 是整篇抽文本，附带页数和元信息，空 PDF 直接报错。网页会先删脚本样式导航页眉页脚，再优先取正文标签，取不到才取全身，空白折叠后仍为空就报错。纯文本直接读，空文件报错。JSON 支持三种形态：数组、包了文档数组的对象、单个文档对象，缺内容就报错。批量上传是逐个处理，单个失败只记错误不影响其他。

深挖：扫描版 PDF 为什么不行？以后怎么做？

因为现在只抽文本层，图片型扫描件没有文本层，抽出来是空。如果要做，需要加 OCR，先转图片再识别，或者调多模态模型直接理解版式。表格 PDF 也是坑，抽出来行列会串，需要按版式还原。面试时主动说出无 OCR 是已知边界，会显得诚实。

深挖：网页抓取最容易挂在哪？

一是反爬，没有浏览器头直接被拦，所以带了通用头。二是超时，慢站会拖死请求，所以设了 30 秒。三是正文抽错，把评论区当正文，需要按标签优先级取。现在的清洗是启发式，够用但不完美，生产级可以接可读性算法。

---

## 8. 工具与大模型

### Q23：加一个新工具或者新模型供应商要改几处？

加工具分两步。先按框架的工具函数格式定义好名字、给大模型看的描述和参数结构，描述要写清什么情况下该调它，因为大模型就是靠描述做路由的。再注册到工具注册中心。如果工具要隔离租户，就仿照知识库检索写一个带租户参数的工厂，每次请求现场创建并塞给需要的 Agent。

加供应商主要改模型工厂。走兼容协议的，比如通义千问，只需要加配置和地址；走原生协议的，需要装对应的适配包再加分支。容易漏的是对话接口的校验枚举和前端的类型也要同步加，否则后端支持了前端选不了，这也是本项目目前的一个已知小不一致。

深挖：工具描述写不好会出什么问题？

大模型选工具全靠看名字加描述。描述太泛，什么都调；描述太窄，该调不调；两个工具描述重叠，就会随机选。所以描述要写触发条件和边界，比如知识库工具要写内部文档和知识库关键词，搜索工具要写实时信息和外部事实。调参结构也要把必填可选写死，否则大模型会编参数。

深挖：为什么工具失败要当成正常结果返回，而不是抛异常？

因为智能体的循环是靠看工具结果再想下一步的，抛异常会直接炸断循环，而返回错误文字能让大模型自己换路。比如搜索没配 key，返回缺 key 的说明，大模型会转去查知识库或如实告知。这是一种把异常变成信息的设计，适合 Agent 场景。但编程式调用的工具节点则相反，应该抛出来让图感知失败，两者要区分。

### Q24：联网搜索和知识库检索各有什么取舍？

联网搜索强在实时和广度，弱在口径不可控、结果杂、要花钱。知识库强在口径一致、可追溯，弱在要自己维护、覆盖有限。所以研究员被设定为先内后外，内部命中就引用内部，内部没有或问题明显问新事才联网。两者返回都带来源，方便大模型引用和前端展示。

深挖：搜索参数里那个深度和附带答案是什么意思？

深度是搜得粗还是细，粗的快而便宜，细的慢而全，现在用粗的保证速度。附带答案是让搜索服务先给一段综合摘要，研究员可以直接引用，不用翻多页。条数默认五条，太多会塞爆上下文，太少会漏。每库取几个再全局截断也是同理，都是在召回和 token 之间找平衡。

---

## 9. 登录与多租户

### Q25：登录和租户隔离是怎么做的？

注册时密码用加盐哈希存，登录时比对哈希，比对过就签一个七天有效期的令牌，里面带着用户编号、邮箱、租户编号和角色。

请求进来先过鉴权守卫，从请求头解析出用户，再过租户守卫确认租户存在并透传给下游。业务代码通过装饰器拿到当前用户和租户。之后所有的查库、列知识库、查工作流、取消运行，全部带上租户条件。关系库是行级隔离，向量库是集合名加过滤条件双隔离。当前注册需要前端传租户编号，是按多租户口径设计的，单租户场景可以改成服务端给默认值。

深挖：角色字段存了但没做权限，怎么看？

令牌里确实带了角色，但接口只认是否登录加租户，不认角色，没有管理员和普通用户的区分。这是演示版的简化，生产级要加角色守卫，比如删知识库只能管理员，普通用户只读。面试时承认并给出做法，比说没想过要好。

深挖：注册时让前端传租户有什么风险？

风险是用户可以随便填租户混入别人的租户。演示期为了方便多租户联调才这样，生产级应该是邀请码、域名或管理员分配，注册时校验邀请关系。登录是按邮箱找用户，天然绑定创建时的租户，所以登录反而是安全的。

深挖：密码哈希强度十轮意味着什么？

意味着每次哈希要算几十到上百毫秒，暴力破解成本高，但登录并发高时会吃 CPU。十轮是安全和性能的折中。令牌七天有效，好处是体验好，坏处是泄露后窗口长，生产级要加刷新令牌加黑名单。

---

## 10. 前端

### Q26：前端怎么解析流？内容怎么渲染？

前端是手写的流解析，不依赖第三方流库。拿到响应后用读取器加解码器一点点读，按空行切帧，每帧里找出事件名和数据，结束标记走完成回调，主动关连接走取消回调。

页面状态分成步骤、工具调用和当前文本三块。步骤开始和结束画 Agent 进度条，工具开始、参数、结束、结果画工具卡片，参数和结果过长会截断展示，文本增量拼起来用 Markdown 实时渲染，支持表格、代码块和链接。发送时先乐观插入用户气泡，运行开始事件回来后再回填会话和运行编号，新会话会自动切过去。

深挖：解析时为什么按空行切帧？半帧怎么办？

因为 SSE 规定空行是一帧的结束。网络分片可能导致一次读到半帧，所以要留余量，下次读到再拼。解析失败的碎帧直接忽略，等下一帧补齐。这是流解析的标准做法，否则中文多字节被切断会乱码，靠解码器的流模式保证。

深挖：三个引用分别防什么？

运行编号引用是给停止按钮用的，没有它就发不出显式取消。停止标志是区分用户主动停还是正常结束，决定落库加不加后缀。收尾守卫是保证完成、取消、报错三路只收尾一次，否则半句话会被推进列表两次。这是前端状态机里最容易出重影的地方。

深挖：Markdown 渲染有什么安全和体验坑？

安全上渲染器默认会转义，直接插 HTML 不执行，链接强制新开加防跳转属性，相对安全。体验上代码块和表格要做横向滚动，不然撑破气泡。工具结果只展示前三百字，太长先折叠，保证首屏不卡。

### Q27：切会话、删会话、选工作流有什么细节？

切会话前会先停掉旧流，防止旧流的半句话写进新会话。删除要二次确认。工作流下拉框选好后会随提问一起发过去，不选就是智能路由。输入框回车发送、换行用组合键，生成中按钮会变成停止键。

深挖：切会话不断旧流会出什么事故？

旧流的增量还会往旧状态里拼，但页面已经切到新会话，切回来会发现旧会话多了半截话，新会话也可能被污染。更糟的是旧流落库时用的还是旧会话编号，刷新后旧会话出现一条没头没尾的回答。所以切会话必须先停旧流再切，这是用正确性换一点点等待。

---

## 11. 工程与部署

### Q28：本地开发和生产部署怎么起？

基础设施全部容器化，一条命令拉起数据库、缓存、向量库和附属件。生产是前后端一体，静态页面由后端托管，统一监听一个端口，接口都有统一前缀。本地开发是前后端分开，Vite 另起一个端口并把接口代理到后端。关键环境变量包括签名密钥、模型地址和 key、搜索 key、各中间件地址、跨域域名、默认模型、记忆窗口和阈值。包管理是 monorepo，根命令管后端，过滤命令管前端。

深挖：生产有哪些配置是必须改的？不改会怎样？

签名密钥必须改，不改启动直接报错，这是防弱密钥上线的硬门槛。数据库表自动同步只在非生产开，生产开了会按实体改表，有丢数据风险。跨域默认星号，生产要收敛到前端域名。模型地址和 key 必须配，否则建模型时就是空 key，调用直接失败。

深挖：前端代理和后端托管静态页是什么关系？

本地开发是分离的，前端 dev 服务器代理接口到后端，改代码热更新。生产是一体的，前端先打包成静态文件塞进后端镜像，后端除了接口其他全当静态页返回。这样部署只有一个容器，运维简单，代价是前端发版要重打镜像。接口前缀加静态排除就是为了区分这两类请求。

---

## 12. 场景串讲：三句话能走完全链路

### Q29：如果用户只说你好，会发生什么？

请求先进校验和鉴权，存下用户消息，组装上下文，发现没几条就全量给。主管看了一眼，命中寒暄规则，派给直接回答者。回答者直接调大模型流式吐字，后端翻译成文本三段事件，前端拼气泡。结束时发运行结束，落库助手消息，异步生成标题。整个过程不碰任何工具，所以最快最便宜。

### Q30：如果用户问知识库里的报销流程，会发生什么？

主管看到知识库字眼，直接派研究员。研究员先调知识库检索，遍历当前租户所有库，各取几个再全局排序。如果命中，就带着片段调大模型组织答案并引用来源，流式回前端。如果没命中，再降级调联网搜索，告诉用户这是外部资料。工具调用的开始参数结束结果四段都会展示，用户能看到它确实去查了。

### Q31：如果用户选了工作流再问，会发生什么？

后端查出该租户的工作流，按节点边编译成图。比如先走研究员节点做检索，再走条件节点看结果里有没有关键词，有就走写手 Agent，没有就走搜索工具，最后到结束。每个节点开始结束都会广播步骤事件，前端能看到流程图一步步点亮。单节点挂了会记成报错消息继续走，不会整图重来。

### Q32：如果用户说到一半点了停止，会发生什么？

前端三连：关连接、补发删除、本地收尾。后端三层信号拉起，事件流停掉，当前大模型调用停掉，节点入口检查抛取消异常。收尾时把已吐的半句话加后缀落库，前端也加同样后缀展示。刷新后看到的和当时看到的一致，只是多了已停止的标记。

---

## 13. 高频追问速答

主管会不会死循环：强制三选一加明确结束语义，加上轮数上限和内部调度过滤，基本转不死。

token 成本怎么控：窗口阈值是折中，摘要只压老消息，最近的永远原文，摘要和标题都用低温度短调用，寒暄不走工具。

注入和安全怎么做：向量过滤有白名单，条件只做包含不执行表达式，工具参数有结构约束，取消接口验租户归属。

怎么评测 Agent 好坏：固定问题看路由走没走对、工具命中率、摘要保真度、中断后落库完整性，做成端到端用例定期跑。

横向扩展怎么做：运行表换发布订阅，缓存和存储本就共享，图状态不粘机器，无状态化不难。

为什么不用现成前端套件：协议兼容，手写是为了把步骤和工具分开渲染，随时可换。

如果重来一次最想改什么：工作流保存时加图校验、摘要按覆盖位增量压、运行记录放共享存储、条件节点支持模型判断、加端到端评测，这五件的投入产出比最高。

---

## 14. 一句话背诵卡

双编排是灵魂：问题开放用主管动态派活，流程固定用 DAG 按图施工，两套共用同一套事件和取消语义。

最难的是流式状态机：文本增量、工具参数、国产模型兼容、子 Agent 暂存、取消补发，靠几组集合管去重和收尾。

知识库的关键不是选了哪个向量库，而是租户隔离、无结果降级和中断一致性，这些才是生产可用的分水岭。

取消的本质是分布式协同：浏览器、网关、后端、大模型四方要在百毫秒内达成一致，靠的是双通道加幂等收尾，而不是某一个技巧。

记忆的本质是成本游戏：全量准但贵，窗口便宜但忘事，摘要是在两者之间找平衡，阈值就是那个平衡点。
